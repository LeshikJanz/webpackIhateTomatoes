import * as React from 'react';
const SVG = require('react-svg');

export default class extends React.Component {
  render() {
    return (
      <SVG path="assets/icons/toolbar/strikethrough.svg"/>
    )
  }
}