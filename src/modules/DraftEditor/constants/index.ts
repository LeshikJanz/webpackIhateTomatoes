import { State } from 'slate';

export const INITIAL_SLATE_STATE = State.fromJSON({
  document: {
    nodes: [
      {
        kind: 'block',
        type: 'paragraph',
        nodes: [
          {
            kind: 'text',
            ranges: [
              {
                text: 'A line of text in a paragraph.'
              }
            ]
          }
        ]
      }
    ]
  }
});
export const DONE_TYPING_INTERVAL = 1000;